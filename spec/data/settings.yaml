test_setting: Hello, World!

aws:
  aws_access_key_id: asdf
  aws_secret_access_key: asdf

datasources:
  etcd:
    host: localhost
    port: 4001
  mysql:
    host: localhost

inputs:
  http:
    port: 8082

